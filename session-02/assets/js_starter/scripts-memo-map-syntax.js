// ① array.map(element => 新しい値)

// array.map((element) => {
//   return 新しい値;
// });

// ② array.map((element, index) => 新しい値)

// array.map((element, index) => {
//   return 新しい値;
// });

// ③ array.map((element, index, array) => 新しい値)

// // Object を返す
// map((element) => ({
//   key: value,
// }));

// // 例
// const upperProducts = products.map((product) => ({
//   name: product.name.toUpperCase(),
//   price: product.price,
// }));
